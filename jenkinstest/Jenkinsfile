pipeline {
    agent any

    stages {
        stage ('Compile Stage') {

            steps {
                withMaven(maven : 'Maven_Home') {
                    shell 'mvn -f jenkinstest/pom.xml clean compile'
                }
            }
        }
    
    
    stage ('Testing Stage') {

            steps {
                withMaven(maven : 'Maven_Home') {
                    shell 'mvn test'
                }
            }
        }
        
         stage ('Deployment Stage') {
            steps {
                withMaven(maven : 'Maven_Home') {
                    shell 'mvn deploy'
                }
            }
        }
   
   }         
}